#!/usr/bin/env python
Import('env')

source_files = [
]

objects = []

for source_file in source_files:
    objects.append(env.Object(source_file))
objects.append(SConscript("3ds/SCsub"))
Return("objects")
